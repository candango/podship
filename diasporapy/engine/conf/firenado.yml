app:
  component: engine
  data:
    sources:
        # Set here references from sources defined on data.sources
        - session
        - pod
  pythonpath: ..
  port: 8007

data:
  sources:
    - name: session
      connector: redis
    - name: pod
      connector: sqlalchemy
      url: postgresql+psycopg2://diasporapy:diasporapypass@localhost:5432/diasporapy
      # host: localhost
      # port: 6379
      # db: 0

components:
  - id: engine
    class: diasporapy.engine.EngineComponent
    enabled: true
  #- id: account
  #  class: diasporapy.engine.components.account.AccountComponent
  #  enabled: true
  - id: api
    class: diasporapy.engine.components.api.ApiComponent
    enabled: true
  #- id: user
  #  class: diasporapy.engine.components.user.UserComponent
  #  enabled: true
  #- id: admin
  #  enabled: true
  #- id: info
  #  enabled: true

# Session types could be:
# file or redis.
session:
  type: redis
  enabled: true
  # Redis session handler configuration
  data:
    source: session
  # File session handler related configuration
  # path: /tmp
